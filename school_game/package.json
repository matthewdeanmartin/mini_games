{
  "name": "school-game",
  "version": "1.0.0",
  "private": true,
  "description": "Late Again! A school sidescroller + escape-the-office adventure.",
  "type": "module",
  "scripts": {
    "build": "npm run typecheck && node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
